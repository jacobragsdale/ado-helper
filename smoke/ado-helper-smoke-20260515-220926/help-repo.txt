Manage Git repositories

Usage: ado repo [OPTIONS] <COMMAND>

Commands:
  create  Create a new Git repository in the project
  list    List all repositories in the project
  clone   Clone a repository to the current directory (uses ADO_PAT for auth)
  delete  Delete a repository (permanent — there is no recycle bin)
  help    Print this message or the help of the given subcommand(s)

Options:
      --org <ORG>
          Override the organization URL from config (e.g. https://dev.azure.com/myorg)

      --project <PROJECT>
          Override the default project from config

      --output <OUTPUT>
          Output format

          Possible values:
          - text:  Plain text, one item per line (default)
          - json:  JSON — full API response, useful for scripting
          - table: Column-aligned table (only meaningful for list commands; falls back to text elsewhere)
          
          [default: text]

  -h, --help
          Print help (see a summary with '-h')
